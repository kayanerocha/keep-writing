@model IEnumerable<KeepWriting.ViewModels.HistoriaXCompeticaoVM>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Titulo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Titulo)
        </td>
        <td>
            @Html.RouteLink("Escolher", "EscolherHistoria", new { idCompeticao = item.IdCompeticao, idHistoria = item.IdHistoria, action = "InscreverHistoria" }, new { @class = "text-lg" })
        </td>
    </tr>
}

</table>

@ViewBag.MensagemErro